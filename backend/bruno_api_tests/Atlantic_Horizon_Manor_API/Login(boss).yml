info:
  name: Login(boss)
  type: http
  seq: 1

http:
  method: POST
  url: "{{API_URL}}/admin/login"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
